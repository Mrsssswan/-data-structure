冒泡排序的时间复杂度：
          最好的情况：是排序的元素本身是有序的，只需要比较一轮循环，也就是
          for(int j=arr.length-1; j>i; j--) 这里，arr.length-1 即为n-1
          此时会比较n-1次，即时间复杂度为O[N]
         最差的情况： 排序的元素是逆序的，此时需要比较
               1+2+3+....+（n-1）= n(n-1)/2次，复杂度为O[N^2]
          